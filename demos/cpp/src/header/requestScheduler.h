#pragma once

class requestScheduler {
private:
  /* data */
public:
  requestScheduler(/* args */);
  ~requestScheduler();
};

requestScheduler::requestScheduler(/* args */) {}

requestScheduler::~requestScheduler() {}
